## 시험
시험범위: 1강~ 6강(ppt 참고장표제외)
시험날짜: 10월28일(월요일) 오전10시~ 오전10시50분
연필X, 볼펜으로기입- 주관식및서술형으로구성- 코딩계열문제존재

## 1강
### 웹 프로그래밍

서버 사이드 스크립트 언어를 통해 동적 웹 페이지를 작성하는 것   

### 서버 사이드 스크립트 언어

브라우저에서 요청한 HTML 문서에 서버 사이드 스크립트 언어가 포함되어 있으면, 서버는 이 부분을 자신이 처리한 후 그 HTML 문서를 브라우저에 보내줍니다.

ex) JSP

### 서블릿

클라이언트의 요청을 처리하고 응답을 생성하는 자바 클래스입니다.

따라서 원하는 작업을 수행하려면 서블릿 클래스를 수정하거나 새로운 서블릿 클래스를 작성해야 합니다.

### 서블릿 컨테이너

서블릿을 관리하고 실행하는 역할을 합니다.

1. **JSP 파일 감시**: 서블릿 컨테이너는 JSP 파일의 변경 여부를 감시합니다.
2. **컴파일**: JSP 파일이 변경되면, 서블릿 컨테이너는 해당 JSP 파일을 자바 서블릿 클래스로 컴파일합니다.
3. **클래스 로드**: 컴파일된 서블릿 클래스를 로드하고, 기존 서블릿 객체를 대체합니다.
4. **초기화**: 새로운 서블릿 객체의 init() 메서드를 호출하여 초기화합니다.

이렇게 함으로써, JSP 파일의 변경 사항이 자동으로 반영되어 클라이언트의 요청을 처리할 수 있게 됩니다.

ex) 아파치 톰캣

### JSP

JSP 파일을 작성하면, 서블릿 컨테이너가 이를 서블릿으로 컴파일합니다. 그리고 기존의 서블릿을 대체합니다. 따라서 JSP를 사용하면 서블릿 클래스를 직접 작성하지 않고도 동적인 웹 페이지를 쉽게 만들 수 있습니다.

서블릿이 자바 소스 코드속에 HTML 코드를 넣어 작성한다면, JSP는 이와 반대로 HTML에 자바 코드를 넣는 구조이다.

## 2강

### URL
- 웹 페이지(자원)의 주소/위치
- 자원에 맞는 프로토콜 선언

### URI
- 웹 페이지(자원)의 식별자
- URL의 상위 개념 (URI ⊃ URL)

**URL 형식**

```
프로토콜//서버(도메인) 이름/경로/?쿼리문자열(URI)
```

### DNS

서버(도메인) 이름을 IP(인터넷 프로토콜)로 변환

받은 IP 서버에 페이지 요청

개발자도구


겟, 포스트



### WAS

- 서버의 응용프로그램을 동작하여 비즈니스 로직을 수행 또는 데이터베이스 연동
- 동적인 파일을 서비스
- 웹 서버와 컨테이너의 결합
- 컨테이너 기능의 유무가 웹 서버와의 차이점이며 따라서 그냥 컨테이너라고도 함

> [!NOTE]   
> 자바스크립트는 클라이언트 측에서 동적 기능을 수행, WAS는 서버 측에서 동적 콘텐츠를 생성하여 제공



설정부분: JSP 페이지에 대한 설정 정보
contentType=“text/plain” 일 때의 결과 확인!!!
 contentType에서 charset를 설정하지 않을 때의 결과
확인!!! – “iso-8859-1”
 trimDriectiveWhitespaces=“true” 사용하는 결과 확인!!!
생성부분: HTML 코드 및 JSP 스크립

### JSP 구성요소 7

1. 디렉티브(directive)
2. 스크립트
   - 스크립틀릿(scriptlet)
   - 표현식(expression)
   - 선언(declaration)
3. 표현 언어(expression language)
4. 기본 객체(implicit object)
5. 정적인 데이터
6. 표준 액션 태그(action tag)
7. 커스텀 태그(custom tag)와 태그 라이브러리(JSTL)

### 1. 디렉티브

JSP 페이지의 최상단에 선언하며 페이지 처리 방법 설정

```jsp
<%@ page | include | taglib 속성="값" %>
```

### 2. 스크립트

JSP 페이지에서 자바 코드 사용

**선언문**
JSP 페이지가 서블릿으로 변환될 때 클래스의 멤버로 포함됩니다.

```jsp
<%! 변수/함수 정의 %>
```

**스크립틀릿**
JSP 페이지가 서블릿으로 변환될 때 메서드 내부에 포함됩니다.

```jsp
<% 조건문/반복문 %>
```

**표현식**   
JSP 페이지가 서블릿으로 변환될 때 `out.print()` 메서드로 변환됩니다.

```jsp
<%= 출력할 내용 %>
```

> [!NOTE]
> 스크립틀릿에서 변수나 함수를 정의할 수는 있지만 해당 스크립틀릿 블록 내에서만 유효하며, 표현식 내에서 변수나 함수를 정의하면 에러가 납니다.

### 3. 표현 언어(EL)

표현 언어를 사용하면 굳이 스크립트를 쓸 필요 없음   
스크립트와 마찬가지로 기본 객체 호출 가능

```jsp
${ }
```

### 4. 기본 객체(EL)

### 5. 정적인 데이터(HTML, CSS, JS, 이미지 리소스 등)

### 6. 표준 액션 태그

스크립트, 표현 언어가 많아질수록 JSP코드는 복잡해진다.   
자바로 구현해야 되는 기능을 JSP에서 제공하는 태그로 구현한다.

```jsp
<jsp:forward | include 속성="값">
  <jsp:param 속성="값">
</jsp:forward | include>
```

```jsp
<jsp:useBean 속성="값" />
<jsp:setProperty 속성="값" />
<jsp:getProperty 속성="값" />
```

### 7. 커스텀 태그 & JSTL

표준 액션 태그에 없는 태그를 만들 수 있다.
JSTL는 커스텀 태그를 모아놓은 라이브러리이다.

사용하려면 taglib 디렉티브 태그를 선언해주어야 한다.

```jsp
<%@ taglib prefix="my" tagdir="/WEB-INF/tags/myTags/" %>

<my:tagFile01 속성="값"/>
```

## 3강

기본객체

```
public class MyBean {
    public String getGreeting() {
        return "Hello from JavaBean!";
    }
}
```

```
<jsp:useBean id="myBean" class="com.example.MyBean" scope="request" />
<p>${myBean.greeting}</p>
<%
    // 자바 빈즈의 메서드 호출
    String greeting = myBean.getGreeting();
    out.println("Greeting from JavaBean: " + greeting);
%>
```

### 상태 코드

서버에서 클라이언트에게 알려주는 요청 결과

|상태 코드|설명|
|---|---|
|200|정상 처리|
|307|임시로 리다이렉트(response.sendRedirect() 이용 시)|
|400|요청이 잘못됨(해석 불가)|
|403|요청을 거부|
|404|자원이 존재하지 않음|
|405|요청한 메서드(GET, POST 등)을 거부|
|500|서버 내부 에러(JSP 예외 발생)|
|503|일시적으로 서비스 제공 불가|

## 4강

### <%@ include %>와 <jsp:include> 비교
|비교 항목|<jsp:include> 액션태그|<%@ include %> 디렉티브|
|---|---|---|
|처리시간|요청시간에처리|JSP 파일을JAVA로변환할때처리|
|기능|별도의파일로요청처리|흐름이동현재파일에삽입|
|데이터전달방법|request 기본객체나<jsp:param>을이용한파라미터전달|페이지내의변수를선언한후,변수에값저장|
|용도|화면레이아웃의일부분을모듈화할때주로사용|다수의JSP 페이지에서공통으로사용하는변수를지정하는코드

### 쿠키와 세션의 차이. 
세션Session: 브라우저종료시삭제(기간지정가능)

## 5강

표현언어: 표현식의 기능을 확장.
- 스크립트와 직접적으로 변수 공유 불가능 -> 기본 객체를 통해 주고 받을 수 있음
- 변수, 함수 정의 불가
- 기본 객체의 프로퍼티 사용 가능, 메서드 호출 불가
- 수치연산, 관계연산, 논리 연산가능
- 자바 클래스의 메서드 호출가능
- 람다식 사용 가능
- 스트림 API를 통한 컬렉션 객체 처리가능
- 정적 메서드 실행 가능

el, jstl 모두 태그를 개발자가만들어사용할수있다. -Custom Tag
  Class 작성–TLD 등록– web.xml에TLD 등록– 사

fileRecorder 객체 인코딩 불가능

## 6강

**데이터베이스 관리 시스템(DBMS)**: 데이터베이스를 조작하는 별도의 소프트웨어

MySQL
- 오픈 소스 소프트웨어
- 다중 사용자와 다중 스레드를 제공
- 다양한 운영체제에서 사용 가능
- 여러 종류의 프로그래밍 언어 지원
- 표준 SQL 형식을 지원

> [!NOTE]
> - **다중 사용자**: 여러 사용자가 동시에 응용 프로그램에 접근하고 작업을 수행할 수 있음을 의미합니다.
> - **다중 스레드**: 하나의 응용 프로그램이 동시에 여러 작업(스레드)을 수행할 수 있음을 의미합니다.

데이터베이스프로그래밍의일반적인순서 4

